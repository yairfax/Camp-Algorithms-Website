<title>Admin Login</title>
<div class="card text-center centered" style="width: 18rem">
	<div class="card-header">
		Login
	</div>
	<div class="card-body">
		{{#if fail}}
		<div class="alert alert-danger" style="font-size: 10pt">Username and password not recognized</div>
		{{/if}}
		<p style="font-size: 10pt"><i>Tip:</i> You can register a new admin account from a current admin account.</p>
		<form method="POST" action="/login">
			<div class="form-group">
		    	<input class="form-control" type="text" name="username" placeholder="Username">
		    </div>
		    <div class="form-group">
		    	<input class="form-control" type="password" name="password" placeholder="Password">
		    </div>

		    <button class="btn btn-primary" type="submit">Login</button>
		    <a href="/chugim" role="button" class="btn btn-secondary">Home</a>
		</form>
	</div>
</div>